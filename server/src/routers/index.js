const userRouter = require("./user_router");
const boardRouter = require("./board_router");
const authRouter = require("./auth_router");
const img = require("./img");

module.exports = { userRouter, boardRouter, authRouter, img }